applications:
- nme: nyu-wishlist-service-s20
  path: .
  instances: 1
  memory: 128M
  disk_quota: 1024M
  host: nyu-wishlist-service-s20
  domain: https://nyu-wishlist-service-s20.us-south.cf.appdomain.cloud/
  #command: gunicorn --bind 0.0.0.0:$PORT --log-level=info app:app
  buildpack: python_buildpack
  services:
  - Cloudant
  env:
    FLASK_APP : app:app
    FLASK_DEBUG : false
